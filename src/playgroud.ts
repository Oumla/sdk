import { Oumla } from './client';

const oumla = new Oumla({
    apiKey: '',
    baseUrl: 'https://stg-sandbox.oumla.com',
    env: 'testnet',
});

const myClientShare = '';

const run = async () => {};

run();
