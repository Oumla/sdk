// This file was auto-generated by Fern from our API Definition.

import * as OumlaSdkApi from "../index.js";

export interface CreateNetworkBodyDto {
    /** Human-readable network name (e.g. "ETH Mainnet") */
    name: string;
    /** HTTP RPC URL */
    rpcHttpUrl: string;
    /** Number of decimals for the native token (e.g. 18 for ETH) */
    decimals: number;
    /** Alias name used internally */
    alias?: string;
    /** WebSocket RPC URL */
    rpcWsUrl?: string;
    /** RPC username for Basic Auth (e.g., Bitcoin RPC) */
    rpcUsername?: string;
    /** RPC password for Basic Auth (e.g., Bitcoin RPC) */
    rpcPassword?: string;
    /** EVM networkId */
    chainId?: number;
    /** SLIP-0044 coin type (for BIP44 paths) */
    slip44CoinType: number;
    /** Whether this is a mainnet */
    isMainnet: boolean;
    /** Address family (EVM, BITCOIN, MOVE, etc.) */
    addressFamily: OumlaSdkApi.AddressFamily;
    /** Block explorer URL */
    explorerUrl?: string;
    /** WebSocket explorer URL */
    explorerWsUrl?: string;
    enabled?: boolean;
    /** If true, all organizations can access this network without explicit assignment */
    globalAccess?: boolean;
}
